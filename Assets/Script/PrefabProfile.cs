using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PrefabProfile : MonoBehaviour
{
    public bool tile_on = false;


    // Start is called before the first frame update
    void Start()
    {
        tile_on = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
